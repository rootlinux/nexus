# Development and CI dependencies. Installs the runtime set first, then the tooling that
# must NOT end up in the production image.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

# Test runner. httpx is here rather than in the runtime set because no application module
# imports it — it is pulled in solely by fastapi's TestClient.
pytest==9.0.3
httpx==0.26.0

# Linter. Pinned, and pinned *here* so CI and a developer's local run agree on the rule
# set: ruff enables new rules in minor releases, so an unpinned install turns an unrelated
# PR into a large pre-existing-lint backlog. Bump deliberately, in its own commit.
ruff==0.15.16
